# 업데이트 내역

앱 안에서도 상단 **`업데이트 내역`** 버튼으로 볼 수 있음. 새 버전이면 버튼에 빨간 점이 뜸.

---

## v2.14.0 — 2026-08-13

**🔎 검수 도구 보강**

| 도구 | 내용 |
|---|---|
| `verify-source.js` | 원문 메모 ↔ 데이터 대조가 **아예 안 돌던 것**을 고침(찾던 파일 이름이 `대장장이&화로.txt` 로 남아 있었음). 이제 `추가.txt` 를 읽고 낫·조선장·서고관리인·무림맹주·상점까지 **336항목** 대조 |
| `test-consistency.js` | **새로 추가 (23항목)** — 이름 중복·자기참조·순환 참조, 확률/비용 범위, 비급 도감 ↔ 레시피, 영단 획득처 ↔ 레시피, NPC 카드 ↔ 레시피, README 숫자, **docs/·dist/ 가 소스와 같은지** |

이 검사로 `압축무공정수`(비급이 아니라 재료)를 구분하도록 `kind:'재료'` 를 달았고,
사이트 폴더가 소스보다 오래된 것도 바로 잡혔다.

**📱 모바일 가로 스크롤 버그 수정**

390px 화면에서 문서 폭이 **736px** 로 늘어나 화면 왼쪽이 잘려 보였다.
원인은 CSS 그리드/플렉스 칸의 기본값 `min-width:auto` — 목표 선택 상자에
`찬란한오색금강진연옥(조각3)` 같은 긴 항목이 들어가면서 칸 자체를 밀어내고 있었다.

```css
.wrap > *{min-width:0}
.side,.main{min-width:0}
.row > *{min-width:0}
input,select{min-width:0;max-width:100%}
```

고친 뒤 모든 탭에서 문서 폭 = 화면 폭(390) 으로 맞는다.

**🧪 화면 3종 자동 검사 추가** — `node test-viewports.js`
PC 1440×900 / 태블릿 820×1180 / 모바일 390×844 에서 탭 14개를 전부 열어
① 내용이 그려지는지 ② 가로 넘침이 없는지 ③ 콘솔 오류가 없는지 ④ 새 내용(링크 분류·NPC 묶음)이 보이는지
⑤ 낫 계산이 도는지 확인한다. **108항목.**

> 이전 검사가 이 버그를 놓친 이유: `isMobile:true` 로 열면 `window.innerWidth` 가
> 레이아웃 폭(736)으로 잡혀 "넘쳤는지" 비교가 무의미해진다. 이제 `documentElement.clientWidth` 로 잰다.

**🌐 깃허브 Pages 업로드용 폴더** — `docs/`

- `node build-site.js --out docs --limit 900` → **364MB · 파일 17,362개** (지도 배율 0~6 전부 + 공략 이미지)
- `node test-docs-site.js` — 로컬 http 서버로 띄워 실제 크롬으로 탭 14개·지도·404 확인 (19항목)
- `.nojekyll` 포함, 올리는 방법은 **깃허브-업로드.md**, 안 올라가는 것은 `.gitignore` 참고

---

## v2.13.0 — 2026-08-13

**🔗 관련 링크 4개 추가** (요약 탭 아래 *관련 링크* 카드)

| 분류 | 링크 | 비고 |
|---|---|---|
| 지도 · 자료 | [한월 위키 (노션)](https://sulky-titanium-5df.notion.site/2df34cfb6e8d80c881adcced4bcc73ed?v=2df34cfb6e8d8189b10f000c8300abc6) | 유저 정리 위키 — 아이템·시스템 정보 |
| 랭킹 · 서버 | [한월 랭킹](https://hanwol-rank.skhidc.kr/) | 서버 랭킹 조회 |
| 랭킹 · 서버 | [마인리스트](https://minelist.kr/servers/16703-hanwol.skhidc.kr) | 서버 정보·추천 (접속 주소 `hanwol.skhidc.kr`) |
| 커뮤니티 | [유저 공략 채널](https://discord.com/channels/587152246433775640/1485166398841028749) | **디스코드 로그인 · 서버 가입 후 인증 필요** — 인증 안 하면 채널이 안 보임 |

분류가 3개 → **4개**(커뮤니티 / 지도·자료 / 계산기·도구 / 랭킹·서버)로 늘었다.

**🗂️ NPC 탭 정리** — 카드가 순서 없이 쌓이던 걸 비슷한 것끼리 세 묶음으로 나눴다.

| 묶음 | 내용 |
|---|---|
| ⚒️ 대장간 | 대장장이 제작 · 명인대장장이 제작 · 대장장이 조수 |
| 🧾 그 밖의 제작 NPC | 조선장 · 서고관리인 · 무림맹주 |
| 📖 도감 | 비급 27종 · 영단 19종 |

맨 위 *제작 관련 NPC* 카드 그리드도 **대장간 / 제작 NPC / 퀘스트·기타** 로 묶었다.

**🔨 명인대장장이 제작 18종 한 표로** — 보검 복원 재료 15종(회당 1,000전)과
조각 3개로 되돌리는 3종(회당 100만전)이 같은 **제작** 메뉴라 한 표에 넣고, 후자는 `조각 3개` 배지로 구분.
이 NPC에서 **목걸이 강화**도 가능하다는 안내를 넣었다(강화 수치·비용은 아직 미확인).

**📚 서고관리인 안내 추가** — NPC 제작 목록에 비급이 **안 보이면 조건이 충족되지 않은 것**
(레벨·선행 퀘스트·승급 등. 예: 사혼검결은 화경 승급 후). NPC 탭 안내문과 NPC 카드 설명에 표시.

---

## v2.12.0 — 2026-08-13

**📚 서고관리인 비급 제작 18종** (전부 성공률 100%)

| 비급 | 재료 | 비용 |
|---|---|---|
| 빙설검법 | 강철1 + 무공정수10 | 0전 |
| 압축무공정수 | 무공정수100 + 토끼내단5 + 대장장이의불5 | 0전 |
| 벽력공 | 토끼내단10 | 10,000전 |
| 파천검법 | 토끼내단20 | 10,000전 |
| 섬멸검법 | 단섬검법1 + 강철1 + 자금1 + 무공정수20 | 30,000전 |
| 매화초검 | 목심재1 + 옥장식편1 + 매화옥20 + 무공정수10 | 30,000전 |
| 초살선풍 | 기와문양판1 + 무괴철1 + 갈옥15 + 무공정수30 | 30,000전 |
| 창천검법 | 강오금1 + 백현철1 + 청동각인판1 + 청연광20 + 무공정수30 | 30,000전 |
| 부화검결 | 송진덩어리2 + 신선옥15 + 매화옥15 + 옥장식편1 + 백련강1 + 무공정수20 | 30,000전 |
| 홍매지폭 | 부화검결1 + 홍련업화1 + 철목영지1 + 정적주10 + 무공정수50 | 30,000전 |
| 매화쾌검 | 매화초검1 + 백현철2 + 백련정강1 + 기문부적2 + 강화목1 + 무공정수30 | 30,000전 |
| 혈마검법 | 파력검법1 + 정련실2 + 옥문장식판2 + 설화강철1 + 무공정수50 | 30,000전 |
| 매화중검 | 매화쾌검1 + 초살선풍1 + 설화오금1 + 금향과2 + 녹슨철패5 + 무공정수60 | 100,000전 |
| 천뢰공 | 무림맹비급1 + 벽력공1 + 대장장이의불50 + 금향과1 + 철목영지1 + 무공정수60 | 100,000전 |
| 천살검법 | 섬멸검법1 + 혈사검법1 + 월계엽1 + 한철단조석1 + 기문부적1 + 금강한철1 | 100,000전 |
| 마혼검결 | 녹수정300 + 무림맹비급1 + 압축무공정수10 + 녹슨철패25 + 혈마검법1 + 금강한철3 | 1,000,000전 |
| 폭마공 | 오색진연옥1 + 무림맹비급1 + 압축무공정수20 + 열화신공1 + 천살검법1 + 황수정200 | 2,000,000전 |
| 사혼검결 | 오색금강진연옥1 + 무림맹비급1 + 압축무공정수30 + 고래기름1 + 빙천검법1 + 청수정200 | 3,000,000전 |

**🔗 NPC를 가로지르는 역산** — 사혼검결 1개를 목표로 넣으면
서고관리인 → 대장장이(오색금강진연옥 30% · 오색수정 · 진연옥) → 명인대장장이 재료 15종 →
무림맹주(무림맹비급)까지 22단계가 한 번에 나온다. 필드보스 드랍(빙천검법·혈사검법·파력검법)은
"직접 구해야 하는 재료"로 분리된다.

**📜 비급 도감** 25종 → **27종** (마혼검결·폭마공 추가, 매화중검 제작법 반영).

**🐛 원문 오타 정정** — 파천검법 "도끼내단"→토끼내단, 천살검법 "설멸검법"→섬멸검법,
마혼검결 "무랭맹비급"→무림맹비급. 천살검법 재료의 **혈사검법은 오타가 아니다**(검성 드랍 비급).

**검사**

```bash
node test-core.js        # 449항목
node test-ui.js          # 343항목
node test-integrity.js   # 93항목
```

---

## v2.11.0 — 2026-08-13

**🌾 낫 1~5성 추가**

곡괭이와 같은 승급 구조라 계산 규칙도 같다(실패 시 하위 소모 옵션, 확률 시뮬레이션 포함).
곡괭이와 달리 **1성부터 제작**이라 상점 구매 단계가 없다.

| 단계 | 성공률 | 제작비 | 재료 |
|---|---|---|---|
| 1성 | 100% | 10,000전 | 돌덩어리10 · 철10 · 정철광10 |
| 2성 | 80% | 20,000전 | 1성낫1 + 각 20 |
| 3성 | 60% | 30,000전 | 2성낫1 + 각 30 |
| 4성 | 40% | 40,000전 | 3성낫1 + 각 40 |
| 5성 | 20% | 50,000전 | 4성낫1 + 각 50 |

**⚒️ 대장장이 일반 제작 9종**

| 아이템 | 재료 | 성공률 | 비용 |
|---|---|---|---|
| 탐령구 | 철1 + 적동괴2 | 100% | 0전 |
| 강화주머니(소) | 대장장이의불2 | 100% | 1,000전 |
| 청환단 | 대장장이의불100 | 100% | 50,000전 |
| 열화신공 | 대장장이의불100 | 100% | 50,000전 |
| 오색수정 | 청수적20 + 황수정20 + 적수정20 + 청수정20 + 대장장이의불15 + 설화강철2 | 30% | 5,000전 |
| 진연옥 | 홍련업화1 + 철목영지1 + 매화옥30 + 흑옥30 + 빙옥20 + 일옥10 | 30% | 5,000전 |
| 오색진연옥 | 오색수정2 + 진연옥2 + 기문부적5 + 정련실5 + 옥문장식판5 + 토끼내단15 | 30% | 50,000전 |
| 오색금강진연옥 | 오색진연옥2 + 금강한철2 + 일광용린2 + 한철단조석5 + 설화정련석5 + 금강각인편10 | 30% | 50,000전 |
| 찬란한오색금강진연옥 | 오색금강진연옥2 + 금강한철5 + 홍련업화5 + 철목영지5 + 월계엽5 + 금향과5 | 30% | 500,000전 |

목걸이 3종은 실패해도 **조각 1개**가 남는다.

**💎 명인대장장이 — 목걸이 조각 합성 (전부 100% · 각 100만전)**

| 완성품 | 재료 |
|---|---|
| 오색진연옥 | 오색진연옥조각3 + 기문부적3 + 흉폭한영기2 + 우물영기10 + 황수정100 |
| 오색금강진연옥 | 오색금강진연옥조각3 + 한철단조석3 + 흉폭한영기4 + 우물영기20 + 청수정100 |
| 찬란한오색금강진연옥 | 찬란한오색금강진연옥조각3 + 금강각인편3 + 흉폭한영기6 + 우물영기30 + 적수정100 |

대장장이 제작과 이름이 겹쳐서 목표 목록에는 **"(조각3)"** 이 붙은 이름으로 들어 있다.

**📚 서고관리인 추가**

상점: 월섬검법·단섬검법 각 5,000전.

| 제작 | 재료 | 비용 | 성공률 |
|---|---|---|---|
| 섬멸검법 | 단섬검법1 + 강철1 + 자금1 + 무공정수20 | 30,000전 | 100% |
| 부화검결 | 송진덩어리2 + 신선옥15 + 매화옥15 + 옥장식편1 + 백련강1 + 무공정수20 | 30,000전 | 100% |
| 홍매지폭 | 부화검결1 + 홍련업화1 + 철목영지1 + 정적주10 + 무공정수50 | 30,000전 | 100% |
| 매화초검 | 목심재1 + 옥장식편1 + 매화옥20 + 무공정수10 | 30,000전 | 100% |

**🔧 내부 구조** — 곡괭이 전용이던 계산을 **도구 계열**(곡괭이·낫) 구조로 일반화.
목표 목록·전개도·레시피 도감·제작 순서·확률 시뮬이 전부 자동으로 따라온다.

**원문 오타 정정** — 5성낫의 "동덩어리50" → 돌덩어리50, 찬란한 조각 합성의 "1,00,000전" → 1,000,000전,
오색수정 재료 "청수적20" → **녹수정20**(확인받음).

**검사**

```bash
node test-core.js        # 443항목
node test-ui.js          # 340항목
node test-integrity.js   # 93항목
```

---

## v2.10.0 — 2026-08-13

**⛵ 조선장 — 배장비 제작 추가**

배 장착 장비 4부위(동력=외륜 · 대포 · 갑판 · 그물)를 계산기에 넣었다.

| 단계 | 성공률 | 제작비 | 공통 재료 | 무공정수 |
|---|---|---|---|---|
| 1성 | — | **상점 5,000전** | — | — |
| 2성 | 70% | 10,000전 | 철 1 | 10 |
| 3성 | 60% | 30,000전 | 자금 1 | 20 |
| 4성 | 50% | 50,000전 | 무괴철 1 | 30 |
| 5성 | 40% | 100,000전 | 오금한철 1 | 40 |

여기에 부위별 **명인대장장이 재료**가 단계마다 1개씩 더 들어간다.

| 부위 | 2성 | 3성 | 4성 | 5성 |
|---|---|---|---|---|
| 외륜(동력) | 송진칠료 | 목판 | 접합제 | 기문부적 |
| 갑판 | 와편분말 | 옥장식편 | 강화목 | 옥문장식판 |
| 대포 | 목심재 | 기와문양판 | 청동각인판 | 정련실 |
| 그물 | 송진칠료 | 강화목 | 정련실 | 금강각인편 |

- **목표 목록 · 빠른 버튼(5성 배장비 4종) · 통합검색**에 전부 추가
- 5성외륜 1개를 넣으면 4성→3성→2성과 접합제·기문부적·송진칠료까지 한 번에 역산
- **주작단** — 고래기름 1개 · 100,000전 · 95% (실패 시 현무단 1개)
- **조선장 좌표** x -1023 / y 71 / z -1077 — 지도 웨이포인트(제작 NPC 세트)에 추가.
  지도 주입은 `node inject-map-waypoints.js` 로 갱신됨 (웹맵 웨이포인트 291 → 292)

**🏯 무림맹주 제작 추가**

| 아이템 | 재료 | 비용 | 성공률 |
|---|---|---|---|
| 토벌패 | 토벌석 1 + 무공정수 20 + 정철광 3 | 10,000전 | 100% |
| 시공단 | 흉폭한영기 10 | 0전 | 100% |
| 무림맹비급 | 흉폭한영기 40 | 0전 | 100% |

**🛒 상점 구매가 반영** — 1성 배장비처럼 상점에서 사야 하는 재료는
"직접 구해야 하는 재료" 표에 **상점가**가 뜨고 총 비용에도 합산된다(상점가는 제작비 -10% 대상 아님).

**✅ 부가 옵션이 NPC 제작에도 적용** — 예전에는 곡괭이·화로에만 먹히던
"일반제작성공률증가"(원래 확률 ×1.1)와 "제작비용감소 -10%"가
이제 명인대장장이·대장장이 조수·조선장·무림맹주 제작에도 반영된다.

**📖 레시피 도감**에 NPC 제작 칸 추가 (NPC별로 재료·확률·제작비).

**🐛 고친 것** — 성공률이 `77.00000000000001%` 처럼 나오던 표기.

**원문 오타 정정** — 2성갑판 재료의 "1성외륜" → 1성갑판, 2성그물의 "1성그룸" → 1성그물로 보고 넣었다.

**검사**

```bash
node test-core.js        # 405항목
node test-ui.js          # 322항목
node test-integrity.js   # 93항목
```

---

## v2.9.0 — 2026-08-13

**🏠 메인 화면(홈)**

열자마자 계산기가 나오면 뭘 할 수 있는지가 안 보인다. 이제 **홈**이 먼저 뜬다.

- 한 줄 소개 + **제작 계산 시작 / 전체 검색 / 지도 열기** 버튼
- 숫자 요약 — 광산 65곳 · 웨이포인트 576개 · 부적 189종 · 업데이트 67건
- **바로가기 카드 8개** (제작 계산 · 광산 · 위치 · NPC · 확률 · 장비 · 지도 · 업데이트)
- **최근 업데이트 3건**, 쓰는 요령 4가지
- 홈에서는 목표·옵션 사이드를 숨겨 첫 화면답게 넓게

**🔎 검색 미리보기**

| | 전 | 후 |
|---|---|---|
| 결과를 고를 때 | 목록만 | **아래에 미리보기** — 분류·이름·설명·좌표 |
| 이동 | 눌러서 탭 이동 | 미리보기에서 **탭에서 보기 / 지도에서 보기** |
| 빈 검색창 | 최근 검색어만 | **분류 훑어보기** — 분류별 개수 + 예시 3개 |
| 목록 너비 | 검색창 너비(좁으면 이름이 잘림) | **최소 440px** (화면 좁으면 92vw) |

미리보기는 <kbd>↑</kbd><kbd>↓</kbd> 나 마우스를 올려도 따라 바뀐다.

**단축키 한 칸씩 이동** — 탭이 14개가 되어서:
<kbd>1</kbd>=홈 … <kbd>9</kbd>=NPC·영단 · <kbd>0</kbd>=장비 제작 · <kbd>-</kbd>=확률·부적 ·
<kbd>[</kbd>=위치 · <kbd>]</kbd>=업데이트 · <kbd>\</kbd>=지도

**🐛 고친 것** — 검색 드롭다운이 정의되지 않은 CSS 변수(`--bd`)를 써서 테두리가 글자색으로 나오던 것.

**검사**

```bash
node test-ui.js          # 312항목
node test-integrity.js   # 93항목 (홈 탭·기본 탭·단축키 안내 일치 포함)
node test-browser.js     # 36항목 (홈·검색 미리보기를 진짜 크롬에서)
```

---

## v2.8.0 — 2026-08-13

**✨ 쓰기 편하게 · 보기 편하게**

| 기능 | 어디서 | 설명 |
|---|---|---|
| 글자 크기 | 상단 `가⁻` `가⁺` · <kbd>+</kbd> <kbd>Shift+-</kbd> | 4단계(작게~아주 크게), 저장됨 |
| 표 정렬 | 아무 표나 머리글 클릭 | 다시 누르면 반대 순서. 숫자 열은 숫자로, 글자 열은 가나다로 |
| 표 복사 | 표 위 `표 복사` | 탭 구분 글자로 복사 → 엑셀에 그대로 붙여넣기 |
| 단축키 목록 | 상단 ⌨️ · <kbd>?</kbd> | 실제로 붙어 있는 키만 안내 (검사로 강제) |
| 검색 이동 | <kbd>↑</kbd><kbd>↓</kbd> + <kbd>Enter</kbd> | 결과를 골라서 이동 |
| 최근 검색어 | 빈 검색창 클릭 | 최근 8개 |
| 목표 조합 저장 | 목표 카드 `💾 조합 저장` | 자주 쓰는 목표 묶음을 이름 붙여 저장·불러오기 |
| 되돌리기 | 화면 아래 막대 | 초기화·조합 삭제·불러오기를 되돌림 |
| 지도 좌표 | 좌표칸 | `-969, -965` 통째로 붙여넣기 · <kbd>Enter</kbd> 이동 · 최근 좌표 8개 |
| 인쇄 | 상단 🖨️ · <kbd>Ctrl+P</kbd> | 지금 탭만 깔끔하게 (사이드·버튼·지도 숨김) |
| 맨 위로 | 오른쪽 아래 ↑ | 내리면 나타남 |
| 보기 정리 | — | 표 얼룩무늬 · 탭 묶음 구분선(계산/자료/소식) |

**정렬에서 하마터면 넘어갈 뻔한 것**

한 열에 `"1"` 과 `"1번 발판"` 이 섞이면, 숫자와 글자를 그때그때 다르게 비교해
**순서가 뒤죽박죽**이 된다. 그래서 열마다 성격(숫자 열 / 글자 열)을 **먼저 정하고**
한 가지 방식으로만 비교한다. 숫자 열의 글자 줄은 항상 아래로.

**검사**

```bash
node test-ui.js          # 291항목 (편의 기능 43항목 추가)
node test-integrity.js   # 89항목 (안내한 단축키가 실제로 동작하는지 포함)
node test-browser.js     # 28항목 (스크롤·글자 크기·정렬을 진짜 크롬에서)
```

---

## v2.7.2 — 2026-08-13

**🔍 전체판 지도를 원본 화질로**

배율을 3~6만 담아서 확대하면 흐렸다. 이제 **0~6 전부** 담는다.

```bash
node build-standalone.js --full                      # 전부 (약 490MB, 원본 화질)
node build-standalone.js --full --levels 3,4,5,6     # 가볍게 (약 70MB, 8배 흐림)
node build-standalone.js --full --no-guides          # 공략 이미지 빼기
```

| | 전 | 후 |
|---|---|---|
| 타일 | 576장 (배율 3~6) | **17298장 (배율 0~6)** |
| 원본 용량 | 7.4MB | 321MB |
| 파일 | 70MB | 487MB |
| 확대 화질 | 8배 흐림 | **원본 그대로** |

**만드는 방식도 바꿔야 했다**

- 예전엔 파일 전체를 문자열 하나로 만들어 저장 → **node 문자열 한계 512MB** 에서 깨진다
- 이제 타일을 한 장씩 읽어 **바로 파일로 흘려보낸다** (17298장 3초)
- 브라우저 쪽도 마찬가지 — 지도를 `<script data-embedmap>` **28조각**으로 나눠 담고,
  조각마다 Blob 을 만들어 이어 붙인다 (통짜로 합치면 메모리가 몇 배로 튄다)

**🧪 진짜 크롬으로 여는 검사 추가**

```bash
node test-browser.js     # 19항목 (playwright 없으면 그냥 건너뜀)
```

화면 검사(jsdom)는 `<canvas>` 에 실제로 그려졌는지 못 본다 — v2.7.0 의 빈 지도가 그래서 통과했다.
이건 크롬을 띄워 **픽셀을 세어** 확인하고, 자생지·커서 높이·공략 이미지까지 본다.

**측정** — 487MB 파일: 여는 데 3.4초, 지도 뜨는 데 3.7초, 콘솔 오류 0, 바깥 파일 요청 0

---

## v2.7.1 — 2026-08-13

**🐛 단일 파일에서 지도·이미지가 안 보이던 문제**

지도 타일을 파일 안에 다 넣어 놓고도, 지도는 **바깥 파일 주소로** 타일을 찾고 있었다.
타일 주소를 바꿔치기하는 코드가 **줄바꿈(CRLF/LF) 차이 때문에 조용히 안 걸린 것**.
빌드는 성공했다고 나오고, 열어 보면 지도만 새까맸다.

| 고친 것 | 어떻게 |
|---|---|
| 지도가 빈 화면 | 타일 주소 바꿔치기를 줄바꿈에 안 흔들리는 방식으로 |
| 못 고친 곳을 조용히 넘어감 | 한 곳이라도 못 바꾸면 **크게 알리고 빌드 실패**(exit 1) |
| 없는 배율을 있다고 함 | v2.6.1 의 지도 메타 정리를 **단일 파일에도** 적용 (배율 63개 정리) |
| 가벼운 버전의 죽은 지도 창 | 흰 칸 대신 **안내 + 웹 지도 열기** 버튼 |
| 가벼운 버전의 공략 이미지 | 없는 `guides/` 를 거치지 않고 **바로 원본 주소**로 |

**확인** — 실제 브라우저(Chromium)로 파일을 열어 봤다.

- 지도 타일이 실제로 그려짐 (캔버스 16만 픽셀 전부 채워짐 · 고치기 전 583개)
- 약초 자생지 색칠 19종 · 커서 높이(`Y 63≈`) · 웨이포인트 666개 표시
- 공략 이미지가 내장본(`data:`)으로 뜸
- 가벼운 버전: 바깥 파일 요청 0건 (고치기 전 `map/index.html`·`guides/*` 404)

```bash
node test-standalone.js       # 24항목
node test-standalone-full.js  # 24항목
```

---

## v2.7.0 — 2026-08-13

**📦 지도까지 들어간 단일 파일**

```bash
node build-standalone.js --full                    # dist/한월공략소-전체.html (70MB)
node build-standalone.js --full --levels 2,3,4,5,6 # 더 선명 (약 115MB)
node build-standalone.js --full --no-guides        # 공략 이미지 빼기 (25MB)
node build-standalone.js                           # 지도 없는 가벼운 버전 (0.3MB)
```

파일 **하나**에 전부 들어간다 — 외부 파일 참조 0개.

| 내장 | 수량 |
|---|---|
| 앱 (계산·위치·확률·업데이트·검색) | — |
| 지도 타일 (배율 3~6) | 576장 |
| 약초 자생지 색칠 | 19장 |
| 커서 높이(Y) 데이터 | 2.6MB |
| 공략 이미지 | 34장 |
| 웨이포인트 | 291 + 368 |

**방법**

- 타일·자생지 이미지를 `data:` 주소로 바꿔 지도 페이지 안에 박음 (`window.TILE_EMBED`, `window.OVERLAY_EMBED`)
- 높이 데이터는 `window.MapHeightsData` (fetch 실패 시 쓰는 기존 경로 재사용)
- 그렇게 만든 지도 페이지를 앱 안에 문자열로 넣고 **blob 주소**로 iframe에 띄움
- 공략 이미지는 `window.GUIDE_EMBED` 로 앱에 직접 내장
- blob을 못 쓰는 환경이면 자동으로 기존 파일 방식으로 되돌아감

**검증**: `node _testfull.js` 20항목 — 외부 참조 0개 · 지도 blob 로드 · 타일 576장 · 공략 모달 이미지가 내장본인지까지 확인

---

## v2.6.1 — 2026-08-13

**🐛 업로드용 폴더에서 지도가 안 보이던 문제**

- 원인: 타일을 일부 배율만 담았는데, 지도 데이터(`MAP_DATA`)에는 **모든 배율이 있다고** 적혀 있어서
  없는 타일을 요청하다 화면이 비었다
- 해결: 폴더를 만들 때 **실제로 담긴 배율만 남기도록 지도 메타를 다시 쓴다**
  → 더 확대해도 담긴 배율을 늘려 보여 주므로 **비지 않고 조금 흐릿해질 뿐**
- 확인: 담긴 배율(map/slope/depth 각 3~6)과 실제 파일이 일치하는지 표본 검사

```bash
node build-site.js --out docs     # 깃허브 Pages용 (docs 폴더)
```

---

## v2.6.0 — 2026-08-13

**📦 사이트 업로드용 폴더 자동 생성**

지도까지 포함한 전체 버전을 올리고 싶은데 용량 제한이 있을 때 쓴다.

```bash
node build-site.js                # 20MB 한도 → "사이트 업로드용/" 폴더
node build-site.js --limit 50     # 한도 50MB
node build-site.js --out 폴더이름
```

우선순위대로 한도가 찰 때까지 담는다:
앱 파일 → 지도 타일(멀리서 본 배율부터) → 자생지 색칠 → 커서 높이 → 음영·깊이 → 더 자세한 타일 → 공략 이미지

**20MB 기본 결과 (18.5MB, 606개 파일)**

| 담김 | 용량 |
|---|---|
| 앱 파일 8개 | 0.7MB |
| 지도 타일 레벨 6→3 | 7.3MB |
| 약초 자생지 색칠 | 4.8MB |
| 커서 높이 데이터 | 2.6MB |
| 음영·깊이 레이어 레벨 6→3 | 3.1MB |

뺀 것(지도 레벨 2~0, 공략 이미지)은 폴더 안 **README-업로드.md** 에 "없으면 어떻게 되는지"까지 적힌다.
공략 이미지는 없으면 웹맵 원본에서 자동으로 불러온다.

GitHub Pages 대비 `.nojekyll` 도 같이 만든다.

---

## v2.5.1 — 2026-08-13

**⚡ 약초 자생지 색칠 최적화**

자생지를 여러 개 켜면 지도가 버벅이던 문제.

- 원인: 자생지 이미지가 **7300×7300** 이라 켜둔 개수만큼 매 프레임 확대해서 그렸다 (19개면 19번)
- 해결: 켠 자생지를 **2048×2048 합성 캔버스 한 장**에 미리 합쳐두고, 화면에는 **1번만** 그린다
  - 합성은 켜고 끌 때(또는 이미지가 막 로드됐을 때)만 다시 함
  - 이동·확대할 때는 합성본 한 장만 그리므로 켠 개수와 상관없이 비용이 같다
- 끈 자생지의 원본 이미지는 바로 메모리에서 놓아준다 (전부 끄기도 동일)

| | 전 | 후 |
|---|---|---|
| 프레임당 이미지 그리기 | 켠 개수만큼 (최대 19) | **1** |
| 유지 메모리 | 켠 개수 × 원본 | 합성본 1장 + 켠 원본 |

---

## v2.5.0 — 2026-08-13

**🗑️ 디스코드 로그인(클라우드 저장) 제거**

- `cloud-config.js`, `cloud-sync.js`, `CLOUD-SETUP.md` 삭제. 상단 로그인·저장·불러오기 버튼 제거
- 다시 **서버가 전혀 필요 없는 앱**이 됐다 — 어디에 올려도 그대로 돈다
- 설정은 계속 브라우저에 자동 저장되고, 기기를 옮길 땐 상단 **내보내기 / 불러오기**(JSON) 사용

**🧪 화면 대응 점검 추가**

```bash
node test-responsive.js     # 21항목
```

- 표가 전부 가로 스크롤 상자(`.tw`) 안에 있는지 — 13개 탭 전부 훑어서 확인
- 좁은 화면에서 넘칠 고정 폭·`nowrap` 이 스크롤 밖에 있는지
- viewport·입력 16px·터치 32px·지도 높이 제한·1단 전환·푸터 고정 규칙
- 지도 뷰어의 모바일 분기와 터치 제스처(한 손가락 이동 / 두 손가락 확대)

**🏷️ 제목 정리**

- 상단 제목에서 부제(`대장장이 & 화로 · 재료/비용/시간 역산`) 제거 → **한월 공략소** 만 표시
- 푸터에 남아 있던 옛 이름도 전부 정정

---

## v2.4.1 — 2026-08-13

**📐 푸터 위치 정리**

- 화면이 짧아도 푸터가 **항상 맨 아래**에 붙는다 (sticky footer — `body`를 세로 flex로, 본문이 남는 높이를 차지)
- 본문과 푸터 사이 여백 32px 추가, 푸터 안쪽 여백도 넉넉하게 (모바일 포함)
- 푸터에 남아 있던 옛 이름 `한월RPG 제작 계산기` → **한월 공략소**

---

## v2.4.0 — 2026-08-13

**🔽 사이드 카드 접기**

- **목표 아이템**·**옵션** 카드 제목을 누르면 접었다 펼 수 있다 (▾ 표시가 방향으로 알려줌)
- 접은 상태는 저장돼서 다시 열어도 유지
- 모바일·태블릿에서 사이드가 길어 표를 보기 불편하던 게 해결됨

---

## v2.3.2 — 2026-08-13

**대장장이 조수 — 주문서상자 제작 정리**

| 아이템 | 재료 | 확률 | 비용 |
|---|---|---|---|
| 이류주문서상자 (신규) | 삼류주문서상자 10 + 무공정수 5 | 100% | 0전 |
| 일류주문서상자 (정정) | 이류주문서상자 10 + 무공정수 10 | 100% | 0전 |

- 일류주문서상자 재료가 `삼류` → **`이류`** 로 바뀌고, 그 이류를 만드는 단계가 새로 들어왔다
- 두 단계가 이어져서 **일류 1개 = 삼류주문서상자 100 + 무공정수 60** 으로 자동 역산된다
- 조수 제작 3종 → **4종**, NPC 제작 전체 18종 → **19종**

---

## v2.3.0 — 2026-08-13

**🗑️ 제작 기록(로그) 제거**

- 탭 14개 → **13개**. 관련 함수(기록 추가·통계·CSV)·버튼·상태(`log`, `autoLog`)·CSS 전부 삭제
- 제작 순서 체크 시 자동 기록도 함께 제거
- 예전 저장본에 남아 있는 `log` 데이터는 무시된다 (불러오기 오류 없음)

**📋 제작 순서에 NPC 제작 통합**

- 명인대장장이·조수 제작 단계가 **같은 체크리스트**에 들어감 (재료 → 화로 → NPC 순서)
- 각 단계에 성공률·기대 시도·비용·필요 재료 표시, 완료 개수(`n / 전체`)에 함께 계산
- 제작 순서 탭 아래에 **직접 구해야 하는 재료** 표도 같이 표시

**🐛 고친 것**

- 제작 순서 탭에서 카드 안에 카드가 겹쳐 들어가던 구조 오류
- 로그를 걷어내다 같이 지워졌던 **업데이트 내역 표시 함수** 복구
- 단축키 배열에 남아 있던 `log` 때문에 숫자키가 한 칸씩 밀리던 문제 →
  이제 `1`~`9` = 요약~장비, `0` 확률·부적, `-` 위치, `[` 업데이트, `]` 지도
- 안 쓰이는 상태 필드(`inventory`)와 로그 전용 CSS 잔재 제거

**🧪 무결성 점검 추가**

```bash
node test-integrity.js     # 66항목
```

- 탭 ↔ 패널 ↔ 렌더 함수 매핑이 어긋나지 않는지
- 버튼마다 동작이 연결돼 있는지 (링크 버튼 제외)
- 지워진 기능의 잔재(함수·상태·DOM id)가 남아 있지 않은지
- 앱이 부르는 `game-data` / `craft-core` API가 실제로 있는지
- 문서(README·CHANGELOG)가 현재 버전·기능과 맞는지

**검증**: 코어 385 · UI 241 · 무결성 66 · 높이 30 · 단일파일 19 · 원문 94 — 전부 통과

---

## v2.2.1 — 2026-08-12

**🔗 관련 링크 정리**

흩어져 있던 바깥 링크를 한 카드에 분류해서 모았다. (요약 탭 하단)

| 분류 | 링크 |
|---|---|
| 커뮤니티 | 한월RPG 디스코드 `출처` |
| 지도 · 자료 | HANWOL-WEBMAP 웹 지도 `출처` · 확률 공개 시트 `출처` · 부적표 시트 `출처` |
| 계산기 · 도구 | 약초 조합 계산기 `출처` · [ProDays] 매크로 및 최적화 계산기 · [_Ya_Su] 도깨비 스텟 계산기 |

- 링크마다 **한 줄 설명**
- **`출처`** 배지 = 이 앱이 데이터를 가져온 곳 (배지 없는 건 별개 도구)
- 목록은 `game-data.js` 의 `EXTERNAL_LINKS` — `cat`(분류)·`name`·`url`·`desc`만 넣으면 자동 정렬

---

## v2.2.0 — 2026-08-12

**🎬 성장 가이드 영상**

웹맵의 "한월서버 성장 가이드" 영상 목록을 가져왔다. 요약 탭 하단 카드에서 바로 열린다.

| 영상 | 다루는 내용 |
|---|---|
| 설치 및 접속 가이드 | 디스코드, 전용 런처, 주의사항, 로비 |
| 채널입장부터 튜토리얼까지 | 접속방법, 초반 튜토리얼, 게임 조작법 |
| 성장가이드 [Lv.11~60] | 사냥터, 레벨업, 일류승급, 꿀팁 |
| 성장가이드 [Lv.60~120] | 사냥터, 스텟, 재화, 퀘스트정보, 꿀팁 |

- 카드를 누르면 새 탭에서 유튜브로 열린다
- 통합검색(`Ctrl+K`)에서도 검색됨
- 목록은 `game-data.js` 의 `GUIDE_VIDEOS`

---

## v2.1.2 — 2026-08-12

**🔗 관련 링크**

데이터를 가져온 곳(출처)과 구분해서, 같이 쓰면 좋은 바깥 도구를 따로 모았다.

- 요약 탭 하단 **관련 링크** 카드 + 푸터에 표시
- [[ProDays] 매크로 및 최적화 계산기](https://github.com/Pro-Days/SkillMacro/releases)
- [[_Ya_Su] 도깨비 스텟 계산기](https://yasu2947.github.io/)
- 목록은 `game-data.js` 의 `EXTERNAL_LINKS` — 이름·주소만 넣으면 바로 뜬다

---

## v2.1.0 — 2026-08-12

**📦 파일 하나로 합치기 (IPFS·Pinata 대응)**

`index.html` 만 올리면 `craft-core.js`·`game-data.js` 를 못 찾아 **백지**가 된다.
스크립트를 전부 HTML 안에 넣은 단일 파일을 만들어 해결.

```bash
node build-standalone.js          # → dist/한월공략소.html (326KB, 외부 파일 0개)
node build-standalone.js --out 내파일.html
node test-standalone.js           # 외부 파일 없이 도는지 19항목 검증
```

- 계산기·NPC 제작·위치·약초 조합·확률·부적·업데이트·통합검색 **전부 동작**
- 지도 탭만 타일(321MB)이 없어 "지도 미연결"로 뜨고, 나머지는 정상
- 공략 이미지는 로컬 `guides/` 가 없으면 **웹맵 원본 주소로 자동 대체**

**🔗 디스코드 출처 추가**

- 요약 탭 **데이터 출처** 표에 `한월RPG 디스코드` (초대 링크) 추가 — 출처 6곳
- 푸터·업데이트 탭 출처 표기도 링크로

---

## v2.0.0 — 2026-08-12

**🏷️ 이름 변경: 한월 제작 계산기 → 한월 공략소**

계산기만 있던 게 아니라 지도·위치·확률·업데이트까지 담게 돼서 이름을 바꿨다.
탭 제목·푸터·문서 전부 반영.

**🧙 NPC 제작을 목표로 넣을 수 있음**

- 목표 선택에 **명인대장장이 15종 · 대장장이 조수 3종** 추가
- 넣으면 재료·성공률·**기대 시도**·비용까지 역산. 연쇄 제작도 자동으로 풀린다
  (예: 한철단조석 1개 → 시도 3회 · 접합제 6개(시도 10회) · 송진칠료 10개(시도 10회) · 23,000전)
- 재료가 화로/광산 것이면 기존 계산으로 이어지고,
  아닌 것(향목가루·연마사·봉인된철편 등)은 **직접 구해야 하는 재료** 표로 분리 —
  항아리 좌표와 필요 도구(탐색부적/탐령구)까지 같이 보여준다
- 총 비용 = 화로/대장장이 + NPC 제작비. 요약·제작 순서·요약 복사에 전부 반영

**🗑️ 보유 재료 입력 제거**

- 사이드의 `보유 재료` 카드 삭제 → 목표·옵션만 남아 단순해짐
- 재료 탭은 **필요량 + 산출 광산 + 비중**만 표시 (보유/부족 열, 채우기 버튼 제거)
- 셋(64개) 표기는 그대로

**☁️ 디스코드 로그인 저장 (선택 기능)**

- 디스코드 계정으로 로그인해 설정 전체(목표·옵션·기록·웨이포인트·약초 선택 등)를
  서버에 저장하고 다른 기기에서 불러올 수 있다
- Supabase 무료 플랜 사용, 외부 SDK 없이 REST 직접 호출 (스크립트 추가 다운로드 0)
- `cloud-config.js` 에 URL·anon 키만 넣으면 켜짐. **비워 두면 버튼이 숨겨지고**
  지금처럼 브라우저 저장만 쓴다
- 준비 방법: **CLOUD-SETUP.md** (Supabase 프로젝트 → 디스코드 앱 연결 → 표 SQL → 주소 등록)

**검증**: 코어 371 · UI 241 · 높이/색칠 30 · 원문 대조 94 — 전부 통과

---

## v1.8.0 — 2026-08-12

**📜 비급 · 🗿 동상 · 🪨 비석 (웹맵에서 추가로 가져옴)**

- **비급 25종** — 획득처와 제작 재료를 원본 문구 그대로. `NPC·영단` 탭에서 검색되고
  상단 통합검색(`Ctrl+K`)에서도 잡힌다
  - 제작 재료가 아직 안 적힌 비급은 나중에 `game-data.js`의 `SKILLS`에
    `mats` / `cost` / `p` 를 덧붙이면 표에 그대로 나온다
- **동상 3곳**(한월동상·제천대성·UNKNOWN), **비석 17곳** — 위치 탭 표 + 지도 웨이포인트
  (`[웹맵] 동상`, `[웹맵] 비석` 세트로 따로 껐다 켤 수 있음)
  - 원본에 문수산이 두 번(919/917) 적혀 있어 하나로 합침 (18 → 17)
- 지도 주입 271개 → **291개**, 위치 탭 종류 탭도 9가지로

**검수**

- 웹맵 원본(`js/data.js`)과 **전 항목 대조**: 비급 문구 25건, 동상·비석 좌표,
  사냥터 20, 약초 자생지 38, 항아리 12, 상자 41, 단서 28 — **불일치 0건**
- 자동 테스트: 코어 **352** · UI **234** · 높이/색칠 **30** · 원문 대조 **94**

---

## v1.7.2 — 2026-08-12

**📱 모바일 · 태블릿 대응**

- **태블릿(≤1000px)** — 사이드/본문 1단, 탭은 **가로 스크롤**, KPI·카드 자동 축소
- **모바일(≤640px)** — 카드·표 여백 축소, 표 글자 12px, 터치 버튼 최소 32px,
  입력칸 글자 **16px**(iOS에서 입력할 때 화면이 확대되는 문제 방지),
  지도 높이 **최대 70vh**로 자동, 상단바 부제·여백 정리
- 지도 뷰어는 이미 **한 손가락 끌기 = 이동 / 두 손가락 = 확대·축소** 지원

**📄 푸터 추가**

- 비공식 팬 도구 안내 (게임사·운영진 무관, 게임 문의 안 받음)
- 계산 결과는 **기댓값**이라는 안내
- 입력값은 **이 브라우저에만** 저장되고 서버 전송 없음
- 데이터 출처 링크 5종 + 웹맵(FORKY_G) 자료 사용 표기
- **문의 · 오류 제보 · 자료 사용: 디스코드 `79youngsoul`**
- 현재 버전 / 갱신일 자동 표시

---

## v1.7.1 — 2026-08-12

**⚡ 최적화 (웹사이트 배포 대비)**

| 항목 | 전 | 후 |
|---|---|---|
| 입력 1회당 렌더 | 420 ms | **7 ms** |
| 위치 탭 검색 1글자 | 118 ms | **6 ms** |
| DOM 노드 수 | 12,502 | **653** |
| 지도 첫 로딩 | heights.js 3.6MB 동기 로드 | 메타 8KB만, 본체는 지도 만질 때 2.6MB |

- **지연 렌더** — 예전엔 상태가 바뀔 때마다 14개 탭을 전부 다시 그렸다(위치·광산 탭만 HTML 390KB).
  이제 보고 있는 탭만 그린다
- **위치 탭 분할** — 데이터(`#loc-data`)와 웨이포인트 관리(`#loc-wp`)를 따로 그려서,
  검색·필터할 때 필요한 쪽만 갱신
- **저장 디바운스** — `localStorage` 기록을 250ms로 몰아서 1회. 창 닫기·탭 숨김 시 즉시 기록
  (`window.flushSave()`로 강제 기록 가능)
- **높이 데이터 분리** — `heights.js`(메타 8KB) + `heights.bin`(본체 2.6MB).
  웹에서는 `fetch`로, `file://`에서는 `heights-data.js` 사본으로 자동 대체.
  로딩 시점도 **지도에 커서를 올리거나 클릭할 때**로 미룸
- **화면 밖 표는 안 그림** — 긴 표·카드에 `content-visibility:auto`
- 공략 이미지 `decoding="async"` 추가

**📄 DEPLOY.md 추가**

웹사이트로 올릴 때 올릴 파일/뺄 파일, 첫 로딩 용량, 호스팅 선택(GitHub Pages·Cloudflare Pages),
캐시 헤더, **사이트에 반드시 적어야 할 내용**(비공식 표기·출처·원작자·기댓값 안내·갱신일), 갱신 절차 정리.

---

## v1.7.0 — 2026-08-12

**🌿 약초 자생지 색칠 (웹맵과 동일)**

웹맵은 약초마다 자생지를 칠한 7300×7300 투명 PNG를 지도 위에 얹는다. 같은 이미지를 받아와
우리 지도 뷰어에도 겹쳐 그린다. 좌표 변환은 웹맵 `map-logic.js`의 배율·영점을 그대로 썼다.

```bash
node download-overlays.js          # 19종 이미지 → map/overlays/ + map/overlays.js (약 5MB)
node download-overlays.js --force  # 다시 받기
```

- 지도에서 **`Y`키**(또는 툴바 `약초`) → 약초별 체크박스로 켜고 끄기, **전부 켜기/끄기**
- 켠 목록은 브라우저에 저장되어 다시 열어도 유지
- 색칠은 지형 위 · 웨이포인트 아래에 그려서 핀이 가려지지 않음
- 자생지 **점 웨이포인트**(약초별 색)도 그대로 남아 있음 — 둘 다 쓸 수 있음

**📐 좌표 표기 정리**

- 지도 상태바를 **X → Y → Z 순서**로 (`X 1234 · Y 87≈ · Z -567`)
- `≈`는 8블록 평균 높이라는 뜻. 웨이포인트에 올리면 정확한 y

**🖥️ 지도 창 크게 띄우기**

- 지도 탭에 **`큰 창으로 열기 ⛶`** — 화면 90% 크기 새 창, **지금 보던 좌표 그대로** 열림
- **`전체화면`** — 지도 프레임만 전체화면 (한 번 더 누르면 해제)

---

## v1.6.2 — 2026-08-12

**지도에 커서 y(높이) 표시**

지도 타일은 그림이라 높이 정보가 없었다. 원본 프로젝트(`제로소 지도`)의 **Xaero 리전 파일**에는
블록마다 `height` 값이 들어 있어서, 그걸 뽑아 뷰어가 커서 위치의 y를 보여주게 했다.

- 상태바에 `X … · Z … · Y … ≈` 표시 (8블록 평균이라 근사값, `≈` 표시로 구분)
- 웨이포인트 위에 올리면 지금까지처럼 **정확한 y** 표시
- 생성: `python build-heightmap.py` — 리전 1,024개에서 8블록 간격 표본 추출 →
  `map/heights.js` (zlib+base64, **3.6MB**). 이 파일이 없으면 y만 안 뜨고 나머지는 그대로
- 오차 검증(`node test-heights.js`): 광산 65곳 **중앙값 1블록**, 사냥터 2블록, 약초 10블록.
  NPC는 실내·지하가 섞여 있어 차이가 큼(정상)
- 덤: 웹맵 원본에 y가 빠져 있던 **옥향초 2번 자생지**를 높이 데이터로 채움 (y=65)

```bash
python build-heightmap.py            # map/heights.js 생성 (약 6분)
python build-heightmap.py --step 4   # 더 촘촘하게 (용량 4배)
python build-heightmap.py --stats    # 만들지 않고 통계만
node test-heights.js                 # 실제 좌표와 대조 검증
```

---

## v1.6.1 — 2026-08-12

**지도 뷰어 — 웨이포인트 하나씩 켜고 끄기**

- 웨이포인트 패널(`W`)의 목록에 **체크박스** 추가 — 웨이포인트 하나 단위로 지도 표시 on/off
- **전부 켜기 / 전부 끄기** 버튼 — 지금 목록(세트 필터·검색 결과)에 보이는 것만 대상
- 꺼둔 목록은 브라우저에 저장되어 다시 열어도 유지, 패널에 `N개 꺼짐` 표시
- 설정의 `기본값으로`를 누르면 꺼둔 것도 전부 복구

> 세트 단위 on/off(세트 드롭다운)는 그대로. 이제 세트 → 개별 순으로 걸러진다.

---

## v1.6.0 — 2026-08-12

**📢 한월RPG 업데이트 탭 (신규)**

출처: 디스코드 공지 채널 (글만 수록, 이미지·영상 제외)

- 공지 **67건** 수록 — 2026-05-01 ~ 2026-08-12
- **월별로 묶어서** 최신순 표시, 각 글에 날짜·태그 배지
- **태그 필터** — 모드 / 리소스팩 / 퀘스트 / 던전 / 레이드 / 사냥터 / 제작 / 광산 / 약초 /
  스킬 / 부적 / 펫 / 캐시샵 / UI / 오류수정 (본문 키워드로 자동 분류)
- `+`로 시작하는 추가 공지, `업데이트 예정`, `※` 안내를 **배지로 구분**
- 상단 통합검색(`Ctrl+K`)에서도 업데이트 본문이 검색됨
- 탭 단축키 **`]`**

**갱신 방법**

```bash
# 1) 디스코드 공지 글을 복사해 discord-updates.txt 에 붙여넣기
# 2) 변환
node import-discord-updates.js
```

- `작성자 — 2026-05-01 5:43` 형식의 메시지 헤더를 인식하고,
  글 안의 `2026 05 01 업데이트` 같은 날짜 줄이 나오면 **한 메시지 안에서도 항목을 나눔**
- 날짜가 없는 메시지(`— 16:07`)는 본문의 날짜 줄에서 날짜를 찾아냄
- 결과는 `game-updates.js`로 저장 — 이 파일이 없어도 앱은 정상 동작

> 이미지·영상은 포함하지 않음. 첨부까지 넣으려면 DiscordChatExporter의 `--media` 내보내기가 필요하고,
> 남의 서버는 유저 토큰이 있어야 해서 이번엔 **글만** 가져옴.

---

## v1.5.0 — 2026-08-12

**대장장이 조수 NPC**

인게임에서 직접 확인해 넣은 값.

- 🛠️ **장비강화** — 재련 / 잠재능력 / 추가능력 / 주문서강화
- **능력이전** — 잠재능력 이전 **5금화**, 추가능력 이전 **500,000전**
- **귀속해제** — **2금화**
- **제작** — 일류주문서상자(삼류주문서상자10 + 무공정수10, 0전, 100%),
  황동완갑(정포완갑1 + 흉폭한영기4 + 빙백설화3, 50,000전),
  취금완갑(황동완갑1 + 강철3 + 흉폭한영기6 + 무공정수150, 100,000전)
- 좌표는 아직 미확인 — 좌표 없는 NPC는 지도 웨이포인트에서 자동 제외됨

**📍 위치·웨이포인트 탭 (신규)**

출처: [HANWOL-WEBMAP](https://forky-g.github.io/HANWOL-WEBMAP/)

- ⚔️ **사냥터 20곳** — 레벨대·몬스터·좌표
- 🌿 **약초 자생지 19종 38곳** — **자생지마다 약초별 고유 색**으로 표시, 지도 세트도 약초 이름별로 분리
- 🔴🟡🟢 **적환단 10 · 해태단 9 · 기린단 9** 단서 + 세부 위치(발판·열쇠·재료 등) 전부
- 🏺 **탐색 항아리 12곳** — 획득 아이템과 필요 도구(탐색부적/탐령구)
- 🎁 **의문의 상자 41곳** — 내용물·입구 메모
- 📋 **공략 보기** — 해태단·기린단·적환단 공략 이미지 **28건**을 앱 안 모달에서 확인.
  `node download-guides.js`로 34장을 `guides/`에 받아두면 오프라인에서도 보임

**🧪 약초 조합 계산기**

출처: [약초 조합 계산기](https://jeongsh214.github.io/herb_calculator/index.html)

- 약초 3~5개를 고르면 등급 점수 합계(**A+1 · B+2 · C+3 · S+4**)로 결과 환이 나옴
- 3~20점 결과표 전체 표시, 현재 점수 줄 강조, 같은 환이 나오는 다른 점수도 안내

**🔎 전체 통합검색**

- 상단 검색창(**`Ctrl+K`**)에서 재료·제작품·광산·NPC·영단·장비·부적·사냥터·약초·단서·항아리·상자·
  지도 웨이포인트를 한 번에 검색
- 결과를 누르면 **해당 탭으로 이동하면서 그 탭 검색어까지 채워짐**, `지도` 버튼은 좌표로 바로 점프
- `Enter`는 첫 결과, `Esc`는 닫기

**🧭 웨이포인트 편집기**

- 앱에서 웨이포인트 **추가 / 이름·좌표 수정 / 삭제 / 숨김**, 브라우저에 자동 저장
- 가져온(웹맵·지도) 웨이포인트도 수정 가능 — **원본 데이터는 그대로 두고 표시만 덮어씀**,
  `원래대로` 버튼으로 되돌리기
- 출처별(가져온 데이터 / 인게임 지도 / 내가 추가) 필터 + 이름·세트·좌표·메모 검색
- **JSON 내보내기** → `node inject-my-waypoints.js`로 지도에 **`[내]`** 세트로 주입 (`--remove`로 해제)

**🧷 지도 주입 확대**

- 76개 → **271개**, 세트 42종. 광산(색깔별)·NPC·스폰에 사냥터·약초·단서·항아리·상자가 추가됨
- 주입분은 `[웹맵] ` 접두사 유지 → 지도에서 `W`키로 세트별로 껐다 켬
- `import-map-waypoints.js`가 이제 **주입분을 다시 가져오지 않음** (출처 분리 보장)
- 웨이포인트 `관련` 버튼 매핑 확장 — 사냥터·약초·항아리·상자·단서 → **위치 탭**

**🔗 출처 표기**

- 요약 탭 하단 **데이터 출처** 표 — 웹 지도 / 확률 시트 / 부적표 / 약초 계산기 / 인게임 확인,
  각 출처가 어떤 항목을 담당하는지와 확인일까지
- 상단에 **🗺️ 웹 지도 · 🧪 약초 계산기** 바로가기 버튼
- 광산·NPC·확률·부적·위치 카드 제목의 "출처: …"가 전부 **클릭 가능한 링크**로 바뀜

**기타**

- 탭 단축키 **`[`**(위치) 추가, 지도는 `=` 유지
- 테스트 확대: 코어 305 → **329항목**, UI 163 → **205항목**

---

## v1.4.0 — 2026-08-11

**대장장이 VIP**

- 🔥 옵션에 **`대장장이 VIP -10%`** 추가 — 화로 대기시간 감소.
  화력(`0.99^수치`) · `화로시간감소 -10%`와 **곱연산**으로 함께 적용됨
  (예: 화력 50 + 화로시간감소 + VIP = `0.99^50 × 0.9 × 0.9` ≈ **-51%**)
- 화력 힌트에 옵션까지 포함한 최종 배율 표시
- 원문 메모(`대장장이&화로.txt`)에도 `대장장이 VIP / 화로 대기시간 -10% 곱연산` 추가 →
  `verify-source.js`가 메모↔데이터를 계속 대조함 (92 → **94항목**)

**새 탭 4개 — 웹맵·확률 시트 데이터 반영**

출처: [HANWOL-WEBMAP](https://forky-g.github.io/HANWOL-WEBMAP/) ·
[확률 공개 시트](https://docs.google.com/spreadsheets/d/1bXZ8gICXNbS6Wn0z-YfMnqHLxjpEbSnrWbf854Lj9xY/edit)

- 🗺️ **광산** — 광산 65곳 좌표, 색깔별 산출 재료, 색깔별 추천 순회 동선.
  맨 위에 **"이번 목표에 필요한 광산"** 표가 있어서, 부족한 재료가
  *어느 색 광산의 몇 번*에서 나오는지 계산 결과와 바로 이어짐
- 🧙 **NPC·영단** — 제작 관련 NPC 10곳 좌표·요구 재료·보상,
  **명인대장장이 제작 15종**(재료·확률·비용), **영단 19종** 효과와 획득처. 검색 지원
- ⚔️ **장비 제작** — 50/80/110/150/180제 방어구·무기 재료와 제작서 수량,
  제작대 티어 확률, 장신구(반지·귀걸이·목걸이) 옵션.
  **버튼 한 번으로 장비 재료를 목표에 추가**(방어구는 4부위 ×4도 지원)
- 🎯 **확률·부적** — 잠재능력/추가능력 확률과 수치 범위, 확인 줄 수 확률,
  제작대 티어, 토벌의뢰, 우물던전 아이템·혈석, 무기외형상자,
  **부적 174종**(일반24/고급39/희귀57/영웅36/전설18) 등급별 확률. 부적 이름 검색

**웨이포인트 출처 분리 (지도 연동 대비)**

- 📌 좌표 데이터에 **origin 태그** 도입 — `webmap`(가져온 것) / `map`(인게임 지도 웹뷰어)
- 두 출처는 **별도 배열**로 관리되고 절대 한 목록에 합쳐지지 않음.
  `waypoints(origin)`으로 필요할 때만 태그 붙여 조회
- `setMapWaypoints()`로 지도 프로젝트 웨이포인트를 **주입만** 받음 (저장은 지도 쪽 책임).
  교체 방식이라 누적되지 않고, 좌표가 숫자가 아니면 무시
- `waypoints()`는 사본을 반환해서 결과를 고쳐도 원본이 오염되지 않음
- 광산 탭에 **"웨이포인트 출처"** 표 추가 — 출처별 개수, 미연결 표시

**인게임 지도(Xaero) 연동**

- 🧭 `import-map-waypoints.js` 추가 — 지도 프로젝트의 `out/index.html`에서
  `window.MAP_DATA`를 파싱해 **`map-waypoints.js`를 생성**.
  현재 오버월드 **웨이포인트 361개 / 세트 38종** (사망 지점 7건은 기본 제외)
- 앱이 `map-waypoints.js`를 자동으로 읽어 `origin: map`으로 주입.
  **파일이 없어도 앱은 그대로 동작**한다
- 광산 탭에 **지도 웨이포인트 카드** — 세트별 필터(광산 66 / 약초 61 / 보물 24 / NPC 21 …),
  이름·좌표 검색, 웨이포인트 색 표시
- 좌표마다 **`지도` 딥링크** — `#world/dim/map/layer/x/z/scale` 형식으로 해당 위치를 지도에서 연다.
  전체 광산 65곳 표에도 링크가 붙음
- 📦 **폴더 하나로 통합** — 지도 뷰어를 `map/`에 넣었다 (`map/index.html` + 타일 321MB, 17,299개).
  이제 `제작/` 폴더 하나만 있으면 계산기·지도가 전부 돈다
- 지도가 프로젝트 안에 있으면 딥링크를 **상대경로**(`map/index.html#…`)로 저장 →
  폴더째 옮기거나 다른 PC로 복사해도 링크가 안 깨진다. 밖에 있으면 절대경로로 저장하며 경고
- 🗺️ **지도 탭 추가** — 계산기 안에서 바로 지도를 본다 (iframe).
  x/z 좌표 이동, 스폰 이동, 높이 4단계(420/620/820/1100px), 새 탭에서 열기
  - 표의 **`지도`** 버튼 → 지도 탭이 그 좌표로 이동. **`↗`** → 새 탭
  - 탭을 열기 전에는 지도를 **불러오지 않는다** (타일 낭비 방지)
  - render()가 여러 번 불려도 **iframe을 다시 만들지 않는다** — 타일 재로딩 방지
  - 첫 위치는 스폰 지점, 마지막 좌표·높이는 저장됨
  - 단축키 `=`
- 🧷 **웹맵 좌표를 지도 웨이포인트로 주입** — `inject-map-waypoints.js`.
  광산 65 + 제작 NPC 10 + 스폰 1 = **76개**를 지도 데이터에 넣는다
  - 세트 이름이 **`[웹맵] `** 으로 시작하고 광산은 **색깔별로 분리** —
    녹 24 / 청 14 / 황 12 / 적 15, 제작 NPC 10, 스폰 1
  - 지도의 **`W`키**(웨이포인트 패널)에서 세트별로, **`O`키**(설정)에서 전체를 **껐다 켤 수 있다**
  - **여러 번 돌려도 안전** — 주입분(`wp.webmap`)을 매번 걷어내고 다시 넣는다.
    `--remove`로 주입분만 해제
  - **지도에서 직접 찍은 웨이포인트 368개는 건드리지 않는다**
- 🔀 **관련 탭 이동** — 웨이포인트 표의 `관련` 버튼으로 대응 탭으로 넘어간다
  (광산→광산, 적환단·기린단·해태단→영단, NPC→NPC, 화로→레시피).
  지도 탭 위쪽에도 광산·NPC·재료·제작순서 바로가기
- 👁️ **지도 웨이포인트 목록 접기** — `목록 표시` 체크박스. 접어도 출처 표·광산 표는 그대로
- 추출기가 `map/` → 옆 폴더 지도 순으로 **자동 탐색**
- 지도를 다시 빌드했으면 `map/`에 덮어쓰고 `node import-map-waypoints.js`만 다시 돌리면 갱신됨
- ⚠️ 지도 광산 66곳 vs 웹맵 광산 65곳 — **개수부터 다르므로 두 목록은 계속 분리**
- 검증 44항목 추가: 주입 후에도 광산 65곳·webmap 개수 불변, 두 목록 상호 오염 없음,
  재주입은 교체, 딥링크 해시 형식·좌표 반올림, 세트 집계 합 = 전체, 사망 지점 제외

**부적 리롤 계산기**

- 🧮 등급을 고르면 **1회 확률 / n회 안에 1번 이상 나올 확률 / 평균 소요 리롤 / 90%·99% 도달 횟수**를 계산
- 고지된 5등급은 전부 `종류별 확률 = 100 ÷ 종류 수` 균등 분포임을 확인 →
  일반 `100/24`, 고급 `100/39`, 희귀 `100/57`, 영웅 `100/36`, 전설 `100/18` 전부 시트 값과 일치
- 여러 종류 중 아무거나 나오면 되는 경우(`원하는 종류` k)도 지원 — `k ÷ 종류 수`
- ⚠️ **시트의 확률은 "등급 안에서 어떤 부적이 나오는지"** — 각 등급 표가 따로 100%라서
  **등급 자체가 뜰 확률은 5등급 전부 미고지**. 부적 탭에 이 경고를 명시함
- 🌟 **신화 등급 추가** — [부적표 시트](https://docs.google.com/spreadsheets/d/1sXR0Dq3tM-S_O94Qu1vA_uXdDxM8cln7AR77iM0WsPw/edit?gid=1075389289)의
  `rank: 신화` **13종**. 확률 고지 시트에 없어서 다른 등급과 같은 규칙으로 산출:
  **100 ÷ 13 = 7.6923%**
  - 천강신력부/유운비영부/청목생령부/자미천운부(힘·민첩·생명력·행운 41~55),
    무극패력부/풍신축영부/불사생맥부/천기개운부(각 % 9.2~12.0),
    멸왕파천부(보스공격력 21.48~28), 파군천격부(공격력 52.32~70),
    투신강림부(공격력% 13.24~17), 만법멸진부(스킬피해량% 11.27~15),
    금강불괴부(체력% 21.48~28) — 전부 15단계
  - 신화 옵션 표를 부적 탭에 별도 카드로 추가
- 부적 목록 기준을 **부적표 시트(최신)** 로 교체 — 174종 → **189종**.
  종류당 확률은 이제 목록에서 자동 계산되고, 고지값과 일치하는지 카드에 표시됨

**부적표 ↔ 확률 시트 불일치 5건** (부적 탭에 표로 표시)

| 등급 | 내용 |
|---|---|
| 신화 | 확률 시트에 등급 자체가 없음 → 13종 기준 7.6923%로 산출 |
| 영웅 | 부적표 38종 vs 시트 36종 (`산토월해`·`월해상룡` 누락) → **2.6316%** (시트 고지값 2.7778%) |
| 전설 | 부적표 `금강불괴격` 자리에 시트는 `풍뢰혈광음`. 18종 동일이라 확률은 같음 |
| 희귀 | `혈영곡`(부적표) vs `혈영고`(시트) — 표기 차이 |
| 영웅 | `무영쇄혼`(부적표) vs `무영쇄흔`(시트) — 표기 차이 |

확률 시트가 구버전으로 보여 **목록은 부적표 기준**으로 넣었음.

**그 밖에**

- 명인대장장이 제작은 곡괭이처럼 **회당 1,000전**이 들어감 → 성공 1개당 기대비용도 함께 표시
- 탭 단축키 `1`~`9` · `0` · `-` 로 확장
- `game-data.js` 신규 — 광산/NPC/영단/장비/확률/부적 데이터 분리
- 검증 51항목 추가: 광산 번호 중복·좌표 타입, 계산기 광산 재료 매핑 누락,
  색깔별 동선이 해당 색 광산을 전부 포함하는지, 확률 표 합계 100%,
  부적 등급별 `종수 × 확률 = 100%` 자동 대조

## v1.3.0 — 2026-08-09

**셋(64개) 단위 표기**

- 🧺 **64개 = 1셋**. 필요 / 보유 / 부족 수량이 `89셋 4개` 형태로 표시
- 보유 재료 입력이 **셋 / 낱개** 두 칸 — `2셋 + 5개` 넣으면 133개로 합산
- 한 셋 개수는 옵션 패널에서 변경 가능 (64가 아닌 게임에도 대응)
- 적용 범위: 재료 탭(필요·보유·부족), 요약 KPI, 중간 제작물, 제작 순서 재료,
  확률 시뮬 재료 분포, 요약 복사, 채집 목록 복사
- 숫자에 마우스를 올리면 원래 개수(`5,700개`)가 툴팁으로 나옴
- 셋 표기는 체크박스로 끌 수 있음 (끄면 기존처럼 개수만)

**검수**

- 🔍 `verify-source.js` 추가 — 원문 메모를 직접 파싱해 데이터와 **90항목 자동 대조**.
  메모를 고친 뒤 이걸 먼저 돌리면 반영 누락이 바로 잡힘
- 같은 밀리초에 기록이 여러 건 쌓이면 삭제 시 함께 지워지던 문제 수정
- JSON 불러오기에 손상 데이터 방어 추가 (배열·객체 타입 검사, 로그 항목 정규화)

## v1.2.0 — 2026-08-09

**원문 메모 수정본 반영**

- 📌 **오금한철 재료 정정** — `강오금6` → **`오금철5 + 강오금1`**
  (기존 메모에 `강오금5 … 강오금1`로 적혀 있어 합산했으나, 실제로는 앞이 `오금철5`였음)
- 5성곡괭이 제작비 `300,000전`, 설화오금 `백현철1` — 추정이었던 값이 원문에서 확정됨
- 원문 해석 메모에서 해결된 오탈자 항목 제거

**계산 결과 변화** (5성곡괭이 1개, 옵션 없음 기준)

| 항목 | v1.1.0 | v1.2.0 |
|---|---|---|
| 화로 직렬 시간 | 44일 5시간 | **39일 5시간** |
| 화로 제작 횟수 | 8,791회 | **8,191회** |
| 철광석 | 5,260 | **4,860** |
| 갈옥 | 3,404 | **3,004** |
| 돌덩어리 | 3,110 | **2,910** |
| 청연광 | 2,896 | **2,496** |
| 정철광 | 1,702 | **1,502** |
| 매화옥 | 748 | **548** |
| 총 비용 | 16,156,000전 | 16,156,000전 (동일) |

## v1.1.0 — 2026-08-09

**제작 기록(로그) 추가**

- 📜 **제작 기록** 탭 신설 — 실제 제작 시도를 성공/실패로 남김
- 현재 목표의 각 곡괭이 단계마다 `성공` / `실패` 원클릭 버튼
- 수동 기록: 아이템 + 결과 + 횟수 + 메모
- 통계: 아이템별 **실제 성공률 vs 표기 확률**, 시도/성공/실패, 누적 지출
- **운 지표** = 실제 성공 ÷ 기대 성공 (100% 초과면 운 좋음)
- 대장장이 기록은 성공·실패 모두 제작비를 지출로 계산 (현재 비용 옵션 반영), 화로는 0
- 제작 순서 체크 시 **자동 기록** (체크박스로 끌 수 있음)
- 기록 CSV 복사 / CSV 파일 내보내기 (Excel용 BOM 포함), 개별·전체 삭제
- 기록은 최대 2,000건 보관, 표에는 최근 300건 표시

**기타**

- 🆕 업데이트 내역 창 추가 (ESC/바깥 클릭으로 닫기)
- 탭 단축키 `1`~`7`로 확장
- 구버전 저장 데이터도 그대로 열림 (로그 필드 자동 보정)

## v1.0.2 — 2026-08-09

- 제작 순서 탭에서 하위 곡괭이 소모량이 실패분을 반영하지 않던 문제 수정
  (3성 1개 만들 때 2성곡괭이가 `×1`로 표시되던 것 → 기대 시도 횟수 기준으로 표시)
- 중간 제작물 표의 잘못된 재료 참조 수정

## v1.0.0 — 2026-08-09

- `대장장이&화로.txt` 메모 기반 최초 버전
- 목표 아이템 → 광산 재료까지 역산, 실패 확률·재료 소모 반영
- 보유 재료 차감 및 부족분 표시
- 화력 / 화로시간감소 반영 시간 계산 (직렬 · 슬롯 병렬 · 최단)
- 제작 순서 체크리스트, 레시피 전개도, 몬테카를로 확률 시뮬레이션, 레시피 도감
- 다크/라이트 테마, 자동 저장, JSON 내보내기·불러오기, 요약 복사

---

## 버전 올릴 때

`index.html`의 `APP_VERSION`과 `CHANGELOG` 배열만 고치면 앱 안 표시가 따라감. 이 파일도 같이 갱신.
