# 깃허브에 웹사이트로 올리기

> **올라간 곳** — 저장소 <https://github.com/79youngsoul/79youngsoul.github.io>
> · 사이트 <https://79youngsoul.github.io/>
> 확인: `node test-live-site.js` (실제 크롬으로 접속해 탭·지도 타일·404까지 17항목)

`docs/` 폴더가 **그대로 올리면 되는 웹사이트**다. 서버 코드·빌드 도구 필요 없음.

- 만드는 명령: `node build-site.js --out docs --limit 900`
- 지금 담긴 용량: **약 364MB · 파일 17,362개** (지도 배율 0~6 전부 + 공략 이미지 34장)
- 검사: `node test-docs-site.js` — 진짜 크롬으로 열어 탭 14개·지도·404를 확인한다

---

## 1. 저장소 만들고 올리기

```bash
cd "D:/백업/한월/제작"

git init
git add .
git commit -m "한월 공략소 v2.14.0"

git branch -M main
git remote add origin https://github.com/<아이디>/<저장소이름>.git
git push -u origin main
```

> 파일이 17,000개라 첫 `push` 는 몇 분 걸린다. 중간에 끊기면 `git push` 를 다시 하면 이어진다.

## 2. Pages 켜기

저장소 → **Settings → Pages**

| 항목 | 고를 것 |
|---|---|
| Source | `Deploy from a branch` |
| Branch | `main` |
| Folder | **`/docs`** |

저장하면 1~2분 뒤 `https://<아이디>.github.io/<저장소이름>/` 로 열린다.

`docs/.nojekyll` 파일이 이미 들어 있다(지킬 처리를 꺼서 파일 17,000개가 그대로 올라가게 함).
**지우면 안 된다.**

## ⚠️ 겪은 문제 2가지 (다음에 또 만나면)

**1) Pages 가 `/docs` 가 아니라 저장소 루트를 서빙**
설정을 `/docs` 로 바꿔도 **마지막 빌드가 변경 전 것**이면 그대로 루트가 나온다.
빌드를 한 번 더 돌리면 반영된다.

```bash
gh api repos/79youngsoul/79youngsoul.github.io/pages/builds -X POST     # 재빌드
gh api repos/79youngsoul/79youngsoul.github.io/pages --jq .source.path  # 설정 확인
curl -so /dev/null -w "%{http_code}" https://79youngsoul.github.io/map/overlays.js   # 200 이면 정상
```

**2) `.gitignore` 규칙 앞에 `/` 를 빼면 사이트 파일이 빠진다**
`guides/` 라고만 쓰면 원본뿐 아니라 **`docs/guides/`** 까지 제외돼 공략 이미지가 안 올라갔다.
`map/tiles/` 도 마찬가지. 반드시 `/guides/`, `/map/tiles/` 처럼 앞에 `/` 를 붙일 것.

확인법:

```bash
# 로컬 docs 와 저장소에 올라간 docs 를 비교 — 아무것도 안 나와야 정상
comm -23 <(find docs -type f | sort) <(git ls-tree -r HEAD --name-only | grep "^docs/" | sort)
```

## 3. 내용을 고친 뒤

`index.html` · `game-data.js` 같은 소스를 고쳤으면 사이트 폴더를 다시 만들어야 반영된다.

```bash
node test-core.js && node test-ui.js && node test-integrity.js   # 먼저 검사
node build-site.js --out docs --limit 900                        # 사이트 폴더 다시 만들기
node test-docs-site.js                                           # 진짜 크롬으로 확인
git add -A && git commit -m "내용 갱신" && git push
node test-live-site.js                                           # 1~3분 뒤 실제 사이트 확인
```

## 용량을 줄이고 싶으면

`--limit` 을 낮추면 **먼 배율 타일부터** 담고 자세한 배율은 뺀다(지도가 안 비고 조금 흐릿해질 뿐).

| 명령 | 대략 용량 | 지도 |
|---|---|---|
| `node build-site.js --out docs --limit 20` | 20MB | 멀리서 보는 배율만 |
| `node build-site.js --out docs --limit 200` | 190MB | 확대 대부분 가능 |
| `node build-site.js --out docs --limit 900` | 364MB | **전부 (원본 화질)** |

빠진 항목은 `docs/README-업로드.md` 에 적힌다.

## 올라가지 않는 것 (`.gitignore`)

| 빠지는 것 | 이유 |
|---|---|
| `dist/` | 전체판 단일 파일이 487MB — 깃허브 파일 1개 100MB 제한 초과 |
| `map/map`, `map/depth`, `map/slope`, `map/overlays`, `map/heights.js` | 원본 타일 321MB. 필요한 만큼 `docs/` 안에 이미 복사돼 있음 |
| `guides/` | 공략 이미지 원본. `docs/guides/` 로 복사되어 올라감 |
| `node_modules/`, `versions/`, `shot-*.png` | 다시 만들 수 있는 것 |

USB로 옮길 때 쓰는 `dist/한월공략소-전체.html` 은 깃허브 대신 **파일 그대로 복사**해서 쓰면 된다.

## 다른 곳에 올릴 때

- **Cloudflare Pages / Netlify** — `docs/` 폴더를 드래그해서 올리면 끝. 깃허브보다 업로드가 빠르다
- **파일 하나만 올려야 하는 곳(IPFS 등)** — `dist/한월공략소.html`
