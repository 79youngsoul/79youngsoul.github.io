# 폴더 안내 — 뭐가 필요하고 뭐가 없어도 되는지

`index.html` 을 더블클릭하면 바로 실행됩니다. 아래는 각 파일이 왜 있는지 정리한 것.

---

## 1. 앱이 도는 데 꼭 필요 (지우면 안 됨)

| 파일 | 하는 일 | 없으면 |
|---|---|---|
| `index.html` | 앱 본체 (화면·계산·검색 전부) | 앱이 아예 안 열림 |
| `craft-core.js` | 곡괭이·화로 레시피와 계산 로직 | 백지 화면 |
| `game-data.js` | 광산·NPC·위치·확률·부적·약초·비급 데이터 | 백지 화면 |

## 2. 있으면 좋은 것 (없어도 앱은 돌고, 해당 기능만 빠짐)

| 파일 / 폴더 | 하는 일 | 없으면 | 다시 만들기 |
|---|---|---|---|
| `map-waypoints.js` | 인게임 지도에서 뽑은 웨이포인트 368개 | 광산 탭의 지도 웨이포인트 목록만 빔 | `node import-map-waypoints.js` |
| `game-updates.js` | 한월RPG 업데이트 내역 67건 | 업데이트 탭에 안내만 뜸 | `node import-discord-updates.js` |
| `map/` | 지도 뷰어 + 타일 + 높이 + 자생지 색칠 (333MB) | 지도 탭이 "미연결" | 지도 프로젝트에서 다시 빌드 |
| `guides/` | 공략 이미지 34장 (34MB) | 웹맵 원본 주소에서 불러옴 (인터넷 필요) | `node download-guides.js` |

## 3. 배포용 결과물 (필요할 때만 쓰고, 지워도 다시 만들 수 있음)

| 폴더 | 내용 | 다시 만들기 |
|---|---|---|
| `docs/` | **사이트 업로드용 완성본** (363MB · 지도·공략 전부 포함) | `node build-site.js --out docs --limit 500` |
| `dist/한월공략소.html` | **가벼운 단일 파일** (0.3MB) — 지도 탭은 안내 + 웹 지도 링크, 공략 이미지는 인터넷에서 | `node build-standalone.js` |
| `dist/한월공략소-전체.html` | **전부 들어간 단일 파일** (487MB · 지도 배율 0~6 원본 화질 · 공략·자생지·높이 포함) | `node build-standalone.js --full` |
| ↳ 가볍게 | 같은 파일을 70MB로 (확대하면 흐림) | `node build-standalone.js --full --levels 3,4,5,6` |
| `versions/` | 버전별 백업 스냅샷 (14MB) | `node snapshot.js` |

> 용량이 부담되면 `docs/` 를 지웠다가 올릴 때 다시 만들면 됩니다.
> `--limit 20` 처럼 한도를 주면 20MB짜리로도 만들 수 있습니다.

## 4. 데이터 갱신 도구 (게임이 바뀌었을 때만 실행)

| 파일 | 언제 쓰나 |
|---|---|
| `import-discord-updates.js` | 디스코드 공지 새로 올라왔을 때 (`discord-updates.txt` 에 붙여넣고 실행) |
| `import-map-waypoints.js` | 지도를 새로 빌드했을 때 (지도 → 앱으로 웨이포인트 가져오기) |
| `inject-map-waypoints.js` | 앱의 위치 데이터를 지도에 웨이포인트로 넣기 (291개) |
| `inject-my-waypoints.js` | 앱에서 내보낸 내 웨이포인트를 지도에 넣기 |
| `download-guides.js` | 공략 이미지 받기 (한 번이면 됨) |
| `download-overlays.js` | 약초 자생지 색칠 이미지 받기 (한 번이면 됨) |
| `build-heightmap.py` | 지도 새로 빌드했을 때 커서 높이 데이터 다시 만들기 (약 6분) |

## 5. 검사 도구 (고친 뒤 확인용)

```bash
node test-core.js          # 계산·데이터 386항목
node test-ui.js            # 화면 동작 312항목
node test-integrity.js     # 탭·버튼·API 연결 93항목
node test-responsive.js    # PC·태블릿·모바일 21항목
node test-heights.js       # 지도 높이·자생지 33항목
node test-standalone.js       # 단일 파일 27항목
node test-standalone-full.js  # 지도 포함 단일 파일 14항목
node test-browser.js          # 진짜 크롬으로 열어 보기 36항목 (playwright 필요)
node verify-source.js      # 원문 메모 ↔ 데이터 94항목
```

`build-site.js` · `build-standalone.js` · `snapshot.js` 는 위 3번 결과물을 만드는 도구.

## 6. 원본 자료 (참고용, 앱은 안 씀)

| 파일 | 내용 |
|---|---|
| `대장장이&화로.txt` | 레시피 원문 메모 — `verify-source.js` 가 이걸로 데이터를 대조 |
| `discord-updates.txt` | 디스코드 공지 원문 (붙여넣은 것) |
| `README.md` · `CHANGELOG.md` · `DEPLOY.md` | 설명서 · 업데이트 내역 · 배포 안내 |

## 7. 없어도 그만

| 대상 | 설명 |
|---|---|
| `node_modules/` | 화면 테스트(jsdom)용. 지워도 앱은 정상. 다시 필요하면 `npm i jsdom` |

---

## 자주 하는 일 요약

| 하고 싶은 것 | 명령 |
|---|---|
| 그냥 쓰기 | `index.html` 더블클릭 |
| 파일 하나로 들고 다니기 | `node build-standalone.js --full` → `dist/한월공략소-전체.html` (지도 원본 화질, 487MB) |
| 가볍게 들고 다니기 | `node build-standalone.js` → `dist/한월공략소.html` (0.3MB) |
| 사이트에 올리기 | `node build-site.js --out docs --limit 500` → `docs/` 통째로 업로드 |
| 공지 갱신 | `discord-updates.txt` 에 붙여넣고 `node import-discord-updates.js` |
| 버전 백업 | `node snapshot.js --note "무엇을 바꿨는지"` |
